rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allows authenticated players and hosts (including anonymous users)
    // to create, read, and update live game sessions.
    match /games/{gameCode} {
      allow read, write: if request.auth != null;
    }
  }
}
